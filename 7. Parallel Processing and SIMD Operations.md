# Parallel Processing and SIMD Operations

**Objectives**:
  * Understand Flynn's classification of computer architectures, with emphasis on SIMD and MIMD.
  * Demonstrate parallel processing using multiple functional units and POSIX threads (pthreads).
  * Perform vector operations as examples of SIMD (Single Instruction, Multiple Data) processing.
  * Implement matrix multiplication using SIMD instructions.
  * Analyze performance improvements achieved through parallel execution and SIMD operations.


**Theory**:

**Flynn's Classification**

Flynn's classification divides computer architectures into four categories based on the number of instruction streams and data streams:

* **SISD (Single Instruction, Single Data)**:
    * This refers to traditional sequential processors where a single instruction operates on a single data element at a time. Most conventional uniprocessor systems fall into this category. SISD serves as the baseline for comparing parallel architectures.

* **SIMD (Single Instruction, Multiple Data)**:
    * Executes the same instruction on multiple data elements simultaneously. This is commonly seen in vector processors and GPU architectures. SIMD is highly efficient for data parallel tasks such as image processing, scientific computation, and multimedia applications. Modern processors include SIMD instruction sets such as Intel SSE/AVX (x86) and ARM NEON, which enable processing of 4, 8, or more data elements in a single operation.

* **MISD (Multiple Instruction, Single Data)**:
    * A rare architecture where multiple instruction streams process a single data stream. Systolic arrays and certain fault-tolerant systems are sometimes cited as examples.

* **MIMD (Multiple Instruction, Multiple Data)**:
    * Supports the parallel execution of multiple independent instructions on different data elements. Modern multicore processors and distributed computing clusters are examples of MIMD architectures. This paradigm is fundamental to multithreaded programming and parallel computing frameworks.


**Parallel Processing**

Parallel processing involves using multiple functional units or processor cores to perform tasks simultaneously, which improves computational speed and throughput. By dividing a problem into smaller sub tasks and executing them concurrently, the overall execution time is significantly reduced.


* **Thread Level Parallelism**: Multiple threads execute independently, each handling a portion of the workload. This is achieved through multithreading libraries such as POSIX threads (pthreads).

* **Task Decomposition**: A computational problem is divided into independent tasks that can be processed concurrently without dependencies.

* **Synchronization**: When threads share resources, synchronization mechanisms (such as mutexes) ensure data integrity and prevent race conditions.

* **Speedup**: The ratio of sequential execution time to parallel execution time. Ideally, with N threads, speedup approaches N, though overhead and synchronization limit practical gains.



**Vector Processing and SIMD**

Vector processing applies a single instruction to multiple data points simultaneously, which significantly enhances computational efficiency. Instead of processing one element at a time (scalar processing), vector processors operate on entire arrays in a single operation.

**SIMD Instruction Sets**:

* **x86 Architecture**: SSE (Streaming SIMD Extensions) and AVX (Advanced Vector Extensions) provide 128 bit and 256 bit vector registers capable of processing 4 or 8 single precision floats simultaneously.

* **ARM Architecture**: NEON provides 128 bit vector registers for parallel processing on ARM based systems.


---

> The steps from the Source Code section to the Run section are not required to be included in the lab report.

**Source Code**:

The source code demonstrates Flynn's classification (SISD, SIMD, MISD, MIMD), parallel processing with pthreads, SIMD vector addition, and SIMD matrix multiplication.

> Follow the link: [Source Code](https://github.com/techgeek68/Computer_Architecture/blob/master/7.1%20Parallel%20Processing%20and%20SIMD.c)

**Compile** (Linux/macOS):
```sh
gcc -o parallel_simd_lab parallelprocessing.c -lpthread 
```

**Run**:

<img width="1141" height="53" alt="Screenshot 2026-04-08 at 8 23 18 AM" src="https://github.com/user-attachments/assets/d10c411e-3e8e-4a13-afaa-f3947d9faa4c" />

```sh
./parallel_simd_lab
```

---

**Results**:

**Flynn's Classification**

* Input:

  * SISD operands (A B): 5 3
  * SIMD Vector A (4 floats): 1 2 3 4
  * SIMD Vector B (4 floats): 5 6 7 8
  * MISD data value: 42
  * MIMD data (4 values): 10 20 30 40

 * Output:
   
<img width="898" height="210" alt="Screenshot 2026-04-09 at 12 01 22 PM" src="https://github.com/user-attachments/assets/6252f6bf-e67e-4841-8c1b-b80adf9c8dfc" />


**SIMD VECTOR OPERATIONS**

* Input:
  * Vector A (4 floats): 1 2 3 4
  * Vector B (4 floats): 5 6 7 8


* Output:

<img width="900" height="166" alt="Screenshot 2026-04-09 at 12 01 31 PM" src="https://github.com/user-attachments/assets/ff5068e4-1bf6-4fd2-b8d5-607714733612" />


**SIMD MATRIX MULTIPLICATION**

* Input:
  * Matrix A (2x2, row-major): 1 2 3 4
  * Matrix B (2x2, row-major): 5 6 7 8

* Output:

<img width="897" height="120" alt="Screenshot 2026-04-09 at 12 01 40 PM" src="https://github.com/user-attachments/assets/d31cc655-6571-4294-932d-1e2e3029217c" />


**PARALLEL PROCESSING WITH PTHREADS**

* Input Data:
  * Inputs (4 values): 1 2 3 4

* Output:

<img width="901" height="196" alt="Screenshot 2026-04-09 at 12 01 50 PM" src="https://github.com/user-attachments/assets/fa7e3deb-fd8e-405b-9876-4d6cce4b7563" />

---

**Discussion & Conclusions**:

This lab demonstrated parallel processing and SIMD operations in modern computer architecture. Flynn's classification was explored through practical examples, with emphasis on SIMD (data level parallelism) and MIMD (task level parallelism via pthreads). SIMD vector operations using SSE/NEON achieved 4× theoretical throughput by processing multiple elements per instruction, while pthread based parallel processing showed speedups approaching the thread count. These results confirm that combining instruction-level (SIMD) and thread level (MIMD) parallelism significantly enhances computational performance for data intensive tasks.

---
